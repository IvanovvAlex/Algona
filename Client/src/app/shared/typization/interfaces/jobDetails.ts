export interface JobDetails {
    id: string,
    title: string,
    description: string,
    responsibilities: string[],
    requirements: string[],
    benefits: string[],
    deadline: string
    
}